import React from 'react';
import {Container} from '@material-ui/core';

export default function Dashboard() {
  return (
    <Container>
      Information about active productions are listed here, etc
    </Container>
  );
}
