import {createTheme} from '@material-ui/core';

const ProprlyTheme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#5271ff',
    },
    secondary: {
      main: '#922d50',
    },
  },
});
export default ProprlyTheme;
