import { createTheme } from "@material-ui/core"

const ProprlyTheme = createTheme ({
    palette: {
        type: 'light',
        primary: {
          main: '#3f51b5'
        },
        secondary: {
          main: '#f50057'
        },
      },
})
export {ProprlyTheme}