import { createTheme } from "@material-ui/core"

const proprTheme = createTheme ({
    palette: {
        type: 'light',
        primary: {
          main: '#3f51b5',
        },
        secondary: {
          main: '#f50057',
        },
      },
})
export default proprTheme